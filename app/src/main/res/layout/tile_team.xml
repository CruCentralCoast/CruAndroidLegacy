<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="org.androidcru.crucentralcoast.presentation.viewmodels.ministryteams.MinistryTeamVM" />
        <variable name="team" type="MinistryTeamVM"/>
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:layout_marginRight="4dp"
        android:layout_marginLeft="4dp"
        android:onClick="@{team.onTeamClick}">

        <ImageView
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:id="@+id/ministry_image"
            android:adjustViewBounds="true"
            app:src="@{team.ministryTeam.mCruImage.mURL}"
            app:tint="@{@color/cruDarkBlue}"/>


    </RelativeLayout>
</layout>